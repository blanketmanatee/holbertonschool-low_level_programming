#include <stdio.h>
/**
 * main - prints name of program followed by new line
 * argc: void
 * argv: argument vecto
 * Return: 0
 */

int main(int argc, char *argv[])
{
	argc = argc;

	printf("%s\n", argv[0]);
	return (0);
}
