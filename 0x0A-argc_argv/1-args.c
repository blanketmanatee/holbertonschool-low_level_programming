#include <stdio.h>

/**
 * main - prints number of args passed to it
 * @argc: count
 * @argv: void
 * Return: 0
 */

int main(int argc, char *argv[])
{
	argv = argv;

	if (argc >= 1)
	{
		{
			printf("%d\n", argc - 1);
		}
	}
	return (0);
}
