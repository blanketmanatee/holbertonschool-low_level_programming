#include <stdio.h>
/**
 *
 * main - prints a string using puts
 * description: print exact quote followed by a new line
 * Return: 0 if success
 */

int main(void)
{
	puts("\"Programming is like bulding a multilingual puzzle");
	return (0);
}
